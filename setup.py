from setuptools import setup, find_packages

setup(
    name='stone_sage',
    version='0.1.0',
    description='Predicting the strength of stone — and the fury of dragons.',
    author='Anita Best',
    packages=find_packages(),
    install_requires=[],
)
